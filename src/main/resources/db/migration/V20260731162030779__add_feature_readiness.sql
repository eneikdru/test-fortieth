-- V20260731162030779__add_feature_readiness.sql
CREATE TABLE features (
    id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    title VARCHAR(255) NOT NULL,
    readiness_ratio DOUBLE PRECISION NOT NULL DEFAULT 0.0
);

ALTER TABLE tasks ADD COLUMN feature_id BIGINT;
ALTER TABLE tasks ADD CONSTRAINT fk_tasks_feature FOREIGN KEY (feature_id) REFERENCES features(id);
